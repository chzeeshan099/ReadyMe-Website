export default function SiteFooter() {
  return (
    <footer className="border-t border-[var(--line-strong)] py-10">
      <div className="shell theme-muted flex flex-col gap-3 text-sm md:flex-row md:items-center md:justify-between">
        <p>Ready Me website scaffolded from the current app experience with responsive web routing.</p>
        <p>Built with Next.js and Tailwind CSS.</p>
      </div>
    </footer>
  );
}

